#231

def n_plus_1(n):
    result = n+1
n_plus_1(3)
print(result)

함수 안의 변수와 함수 밖의 변수가 서로 다른 공간에 있기때문에 에러 발생


#232

def make_url(str):
    domain = "Www." + str + ".com"
    return domain
    
result = make_url("naver")
print(result)

#233

def make_string(str):
      my_list = []
      for i in str:
          my_list.append(i)
      return my_list
      
print(make_string)

#234

def pickup_even(items):
    result = []
    for item in items:
        if item % 2 == 0:
            result.append(item)
    return result

print(items)
